paths-ignore:
  # diagram-render dist bundles Excalidraw's own public Firebase config.
  # The key (VITE_APP_FIREBASE_CONFIG apiKey) belongs to Excalidraw's
  # excalidraw-room-persistence project and is intentionally public in
  # their OSS codebase — Firebase security is enforced via security rules,
  # not by keeping the key secret.
  - "skills/gstack/lib/diagram-render/dist/**"
